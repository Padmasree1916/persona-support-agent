# Multi-factor authentication recovery

Users should first try an unused recovery code or another enrolled authenticator. Device clock drift can invalidate time-based codes; enable automatic date and time and retry once. Support cannot disable MFA based only on an email request.

When no recovery method works, escalate to identity verification. Never ask a user to send a password, full recovery code list, or authenticator seed.

